import { FutureScreen } from '@/components/future-screen';

export default function SettingsScreen() {
  return (
    <FutureScreen
      title="Settings"
      icon="settings-outline"
      message="Business profile and content preferences will be added later."
    />
  );
}
