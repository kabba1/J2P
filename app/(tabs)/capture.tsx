import { FutureScreen } from '@/components/future-screen';

export default function CaptureScreen() {
  return (
    <FutureScreen
      title="Capture"
      icon="camera-outline"
      message="Guided Before, Progress, and After capture is coming in the next milestone."
    />
  );
}
